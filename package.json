{
  "name": "strapi-provider-upload-supabase",
  "version": "1.0.0",
  "description": "Supabase Storage upload provider for Strapi v5 with support for public and private buckets",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "strapi",
    "strapi-provider",
    "upload",
    "supabase",
    "storage"
  ],
  "engines": {
    "node": ">=22.0.0 <=24.x.x"
  },
  "dependencies": {
    "@supabase/storage-js": "^2.81.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "@strapi/strapi": "^5.0.0"
  },
  "license": "MIT"
}
